# Release v5 Siri

## Focus

Hardening del layer Siri per uso reale con Apple Shortcuts.

## Migliorie chiave

- `openUrl` in tutte le risposte principali
- ricerca con stato `resolution`
- agenda con `highPriorityCount`
- `add-note` supporta `query` oltre a `entityId`
- install page aggiornata a v5
- template JSON shortcuts aggiornati con `openField`
